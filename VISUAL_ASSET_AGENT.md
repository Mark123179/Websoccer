# Visual Asset Agent — veraltet

Diese Datei war ein Konzeptdokument aus einer früheren Entwicklungsphase.

Die darin enthaltenen Regeln sind jetzt in `.agents/memory/` gespeichert:

- Asset-Pfade und `fm_inside_id`-System → `.agents/memory/asset-id-system.md`
- Design-Tokens und visuelle Regeln → `.agents/memory/design-tokens.md`
