Build only one component: DashboardHighlightRow.

Do not modify any other page sections.
Do not redesign the entire dashboard.
Only create/adjust the row shown in the reference image.

Goal:
Create a premium futuristic football dashboard row with 5 cards:
- 3 RankingCards
- 1 AbsenceCard
- 1 ManagerDuelCard

The design must closely match the reference image:
- dark navy/teal glass panels
- cyan glowing borders
- rounded futuristic card corners
- subtle inner glow
- large empty dark lower area inside ranking cards
- player entries arranged with overlapping/offset visual composition
- rank badges attached left of each player entry
- player portrait partly inside a polygon/angled frame
- text/value block to the right of the portrait
- no default table layout
- no bootstrap/card generic styling

Important:
This is not a normal list UI.
The RankingCard entries must be custom composed visual layers.

Use:
- CSS Grid for the outer row
- position: relative for each card
- absolute or CSS grid positioning inside the ranking entries
- clamp() for responsive scaling
- aspect-ratio where useful
- CSS variables for spacing, colors and glow intensity

Outer layout:
The row should be one horizontal strip.

Desktop layout:
grid-template-columns: 1fr 1fr 1fr 0.9fr 1.65fr;
gap: clamp(12px, 0.9vw, 22px);
height: clamp(360px, 34vw, 520px);

The first four cards have equal visual height.
The ManagerDuelCard is wider than the others.

Component structure:

<DashboardHighlightRow>
  <RankingCard type="goals" />
  <RankingCard type="assists" />
  <RankingCard type="ratings" />
  <AbsenceCard />
  <ManagerDuelCard />
</DashboardHighlightRow>

RankingCard visual requirements:
- card background: dark teal gradient
- thin cyan border
- subtle cyan glow only on corners/edges
- title row with icon on the left and small chevron on the right
- 3 ranking entries
- entries are vertically stacked but not simple rows
- each entry has:
  - large rank badge on the left
  - player portrait in an angular clipped frame
  - player name to the right
  - metric label below name
  - large cyan value below/next to metric
- rank badge overlaps slightly with the player portrait frame
- player portrait frame should have a polygon/angled look, not a simple circle
- keep lots of dark empty space below the third entry
- do not add bottom buttons

RankingCard entry positioning:
Use a custom layout like this:

.ranking-entry {
  position: relative;
  min-height: clamp(86px, 7vw, 128px);
  display: grid;
  grid-template-columns: clamp(54px, 4vw, 76px) clamp(86px, 6vw, 120px) 1fr;
  align-items: center;
}

.rank-badge {
  position: relative;
  z-index: 2;
  width: clamp(52px, 3.8vw, 72px);
  height: clamp(52px, 3.8vw, 72px);
  clip-path: polygon(0 15%, 75% 15%, 100% 40%, 100% 100%, 20% 100%, 0 80%);
  background: linear-gradient(135deg, rgba(0,220,255,.22), rgba(0,120,150,.12));
  border: 1px solid rgba(0,240,255,.55);
  color: #28f4ff;
}

.player-cutout {
  margin-left: clamp(-12px, -0.7vw, -6px);
  width: clamp(88px, 6vw, 126px);
  height: clamp(76px, 5.4vw, 112px);
  clip-path: polygon(12% 0, 88% 0, 100% 20%, 100% 86%, 86% 100%, 10% 100%, 0 85%, 0 16%);
  overflow: hidden;
  background: rgba(0,30,40,.55);
  border: 1px solid rgba(0,240,255,.35);
}

.player-cutout img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: top center;
}

.metric-value {
  font-size: clamp(24px, 1.8vw, 42px);
  font-weight: 900;
  color: #20f4ff;
  text-shadow: 0 0 12px rgba(0,240,255,.45);
}

AbsenceCard:
- Same general card style as RankingCards
- Title: Ausfälle Gegner
- 3 stacked absence entries
- Each entry uses a darker inner card
- Left: silhouette/player placeholder
- Middle: player name and reason
- Right: injury/suspension icon badge
- No bottom button
- Keep visual style consistent with the row

ManagerDuelCard:
- wider card
- split cyan left side vs gold right side
- top area with two manager portrait circles
- large VS in the center
- names below portraits
- badges below names
- comparison table below
- left values cyan
- right values gold
- middle labels white/gray
- use a diagonal light slash through the center
- no CTA button at the bottom
- no "Zum Duell" button
- bottom should remain clean

ManagerDuelCard layout:
Use 3 columns:
left manager area | center label/stat area | right manager area

The table should have rows:
Titel
Trophäen
Highscore
Registriert seit
Spiele
Siege
Unentschieden
Niederlagen
Punkte pro Spiel

Style hierarchy:
RankingCards and AbsenceCard = cyan theme.
ManagerDuelCard = cyan vs gold split theme.

Do not:
- use standard Bootstrap cards
- use simple HTML tables for ranking cards
- add bottom buttons
- add too many borders
- make all elements equally bright
- make the layout look like an admin dashboard
- replace the custom composition with a normal list

Responsive:
At widths below 1400px, keep the same layout but reduce gap and font sizes with clamp().
At widths below 1100px, allow wrapping:
- first three ranking cards in one row
- absence and duel card in second row
At mobile width, cards stack vertically.

Use placeholder data and placeholder image URLs if needed.
Keep all styling in a dedicated CSS module or component CSS file.